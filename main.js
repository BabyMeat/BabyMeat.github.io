/*
// Sélectionne l'élément contenant le texte à faire clignoter
const textElement = document.querySelector('p');

// Fonction pour faire clignoter le texte
function blinkText() {
    // Si la classe 'blink' est déjà présente, la retire, sinon l'ajoute
    textElement.classList.toggle('blink');
}

// Appelle la fonction blinkText toutes les 500 millisecondes (0.5 secondes)
setInterval(blinkText, 500);
*/